{
  "name": "wishlist-turbo",
  "version": "1.0.0",
  "description": "Netlify function dependencies for web scraping",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "jsdom": "^24.0.0",
    "node-fetch": "^2.6.1"
  }
}
